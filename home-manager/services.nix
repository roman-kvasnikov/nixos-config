{
  services = {
    ssh-agent = {
      enable = true;
    };

    xrayctl = {
      enable = true;
    };

    homevpnctl = {
      enable = false;
    };

    keepassxcctl = {
      enable = true;
    };

    s3fsctl = {
      enable = false;
    };
  };
}
