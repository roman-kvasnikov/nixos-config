{
  services = {
    ssh-agent = {
      enable = true;
    };

    xrayctl = {
      enable = true;
    };

    homevpnctl = {
      enable = true;
    };

    keepassxcctl = {
      enable = true;
    };
  };
}
